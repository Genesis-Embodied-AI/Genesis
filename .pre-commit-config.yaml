repos:
  - repo: https://github.com/psf/black
    rev: 25.1.0  # Use the latest version or specify the version you prefer
    hooks:
      - id: black
        args:
          - --line-length=120
          - .
